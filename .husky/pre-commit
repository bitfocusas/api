#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Format files with biome
npx @biomejs/biome format --write

# Stage formatted files if they were changed
git add -u

# Check for any errors that couldn't be auto-fixed
# This will fail the commit if there are unfixable errors
npx @biomejs/biome check
